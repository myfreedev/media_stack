# ============================================================================
# Media Stack Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# Add .env to your .gitignore file
# ============================================================================

# ----------------------------------------------------------------------------
# Directory Paths
# ----------------------------------------------------------------------------
# Base directory for Docker container data (configs, databases, etc.)
# Example: /home/youruser/docker-data or /opt/docker-data
DOCKER_DATA_DIR=/path/to/docker-data

# Base directory for your media files (movies, TV shows, downloads)
# Example: /mnt/media or /home/youruser/media
MEDIA_PATH=/path/to/media

# ----------------------------------------------------------------------------
# VPN Configuration (Surfshark WireGuard)
# ----------------------------------------------------------------------------
# Your Surfshark WireGuard private key
# Get this from: https://my.surfshark.com/vpn/manual-setup/main/wireguard
# It should look like: aBcDeFgHiJkLmNoPqRsTuVwXyZ1234567890ABCD=
SURFSHARK_WIREGUARD_KEY=your_wireguard_private_key_here

# ----------------------------------------------------------------------------
# Plex Configuration
# ----------------------------------------------------------------------------
# Your Linux username (used for Plex data directories)
# Find with: whoami
USERNAME=youruser

# Your server's IP address (for Plex advertise URL)
# Find with: hostname -I | awk '{print $1}'
# Example: 192.168.1.100
SERVER_IP=192.168.1.100

# Plex claim token (get a fresh one from https://www.plex.tv/claim/)
# This token expires after 4 minutes, so get it right before first setup
# Leave empty if you've already set up Plex
PLEX_CLAIM_TOKEN=claim-xxxxxxxxxxxxxxxxxxxx

# ----------------------------------------------------------------------------
# User/Group IDs (Optional - defaults to 1000)
# ----------------------------------------------------------------------------
# Find your IDs with: id -u (for PUID) and id -g (for PGID)
PUID=1000
PGID=1000

# ----------------------------------------------------------------------------
# Timezone
# ----------------------------------------------------------------------------
# Your timezone (see: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=Europe/London

# ----------------------------------------------------------------------------
# Network Configuration
# ----------------------------------------------------------------------------
# Docker network subnet (should match docker-compose.yml)
DOCKER_NETWORK_SUBNET=172.20.0.0/16

# Docker network gateway
DOCKER_NETWORK_GATEWAY=172.20.0.1

# Your local network subnet (for VPN firewall rules)
# Example: 192.168.1.0/24 or 192.168.50.0/24
LOCAL_NETWORK_SUBNET=192.168.50.0/24
